# Git safety rules

- Use `main` plus feature branches only.
- Never push directly to `main`.
- Pull the latest `origin/main` before merging into `main`.
- Keep repo operations scoped to the correct repository directory.
- Use one worktree per task.
