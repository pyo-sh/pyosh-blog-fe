---
paths:
  - "src/**/*.{ts,tsx,css}"
---

# TailwindCSS v4 rules

- Config entry point: `src/app-layer/style/index.css`
- Custom design tokens use kebab-case such as `bg-background-1` and `text-foreground-2`.
- Use `@apply` only with built-in utilities.
